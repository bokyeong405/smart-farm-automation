# 이 파일을 복사해서 `.env` 로 만들고 실제 값을 채우세요. (.env 는 커밋 금지)
# Naver Clova Voice (TTS) — https://www.ncloud.com 콘솔에서 발급
NAVER_CLOVA_CLIENT_ID=your_client_id
NAVER_CLOVA_CLIENT_SECRET=your_client_secret

# 윈도우 PC(소켓/RoboDK)의 LAN IP
PC_IP=192.168.0.10
